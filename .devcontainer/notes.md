```
nvm install --lts
npm install

# install pi
npm install -g --ignore-scripts @earendil-works/pi-coding-agent

docker exec -it -u node hungry_curran bash
docker exec -it -u node optimistic_lamarr bash
```